name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm install
    - name: Run linting
      run: npm run lint
    - name: Run tests
      run: npm test
    - name: Run build
      run: npx next build
